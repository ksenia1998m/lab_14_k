import { FilterLastNamePipe } from './filter-last-name.pipe';

describe('FilterLastNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLastNamePipe();
    expect(pipe).toBeTruthy();
  });
});
