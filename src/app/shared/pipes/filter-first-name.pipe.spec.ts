import { FilterFirstNamePipe } from './filter-first-name.pipe';

describe('FilterFirstNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFirstNamePipe();
    expect(pipe).toBeTruthy();
  });
});
